#!/usr/bin/env php
<?php

$app = require_once __DIR__ . '/../app.php';

$console = $app['console'];
$console->add(new Toflar\ComposerResolver\Command\ResolveCommand($app['composer-resolver'], $app['predis']));
$console->run();
